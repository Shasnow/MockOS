# 更新日志

本文档记录MockOS项目的所有重要更改。

## [v1.0] - 2024-12-30

### 新增功能
- 初始版本发布
- 实现核心命令：
  - `ls` - 列出目录内容（支持 `-a` 和 `-l` 参数）
  - `mkdir` - 创建目录（支持 `-p` 参数递归创建）
  - `rm` - 删除文件或目录（支持 `-r` 和 `-f` 参数）
  - `cat` - 查看文件内容
  - `cd` - 切换工作目录（支持绝对路径、相对路径、`~`、`..`、`.`）
  - `python3` - Python解释器（支持交互模式、脚本执行、`-c` 和 `-m` 参数）

### 技术特性
- 命令行界面：提供类似Linux的交互式shell
- 路径解析：支持绝对路径、相对路径和PATH环境变量查找
- 环境变量：完整的环境变量系统，支持PATH、HOME等
- Python集成：内置Python3解释器，支持脚本执行和交互式REPL
- 参数解析：使用argparse实现统一的命令行参数解析

### 项目结构
- 建立完整的虚拟文件系统结构
- 实现环境变量管理系统
- 创建核心命令框架

### 文档
- 创建README.md文档
- 添加MIT许可证
- 添加免责声明
